# v1.3.0 plan

Health improvements:
- replace blocked default NTP pool names with configurable reliable servers;
- add Podkop restart recovery if sing-box/Podkop fails after boot;
- add CGNAT/uplink diagnostics;
- preserve existing failover logic.
